# PDF text/word extraction (x-coordinate aware) -- the only extractor actually used.
pdfplumber>=0.11.0

# Data handling & Excel output
pandas>=2.0.0
openpyxl>=3.1.0

# Regex superset (unicode-aware) used by the parser
regex>=2024.0.0

# Optional: only needed if you re-enable the fuzzy college/course reconciliation.
rapidfuzz>=3.6.0
